---
# Production variables for brainbreak

# Domain configuration
domain: bb.learningnow.com.au
app_env: prod

# App configuration based on domain
app_name: "{{ domain | replace('.', '-') }}"
app_user: "{{ app_name }}"
app_root: "/var/www/{{ app_name }}"

# Nginx configuration
nginx_use_ssl: true
nginx_server_name: "{{ domain }}"

# Certbot email for SSL certificates
certbot_email: admin@learningnow.com.au