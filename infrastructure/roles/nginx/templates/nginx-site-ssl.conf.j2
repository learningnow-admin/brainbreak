server {
    listen 80;
    listen [::]:80;
    server_name {{ domain }};
    root /home/{{ deploy_user }}/{{ domain }};
    index index.html index.htm;
    
    location / {
        try_files $uri $uri/ =404;
    }
}